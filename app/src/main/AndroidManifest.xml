<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DOANQLDIEM"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" android:exported="false" />
        <activity android:name=".hosonguoihocActivity"/>
        <activity android:name=".cauhinhActivity"/>
        <activity android:name=".diemActivity"/>
        <activity android:name=".vietdonActivity"/>
        <activity android:name=".create_vietdonActivity"/>
        <activity android:name=".muonphongActivity"/>
        <activity android:name=".create_muonphongActivity"/>
        <activity android:name=".phananhActivity"/>
        <activity android:name=".capnhathosoActivity"/>
        <activity android:name=".thongbaoActivity"/>
        <activity android:name=".thoikhoabieuActivity"/>
        <activity android:name=".hocphiActivity"/>

        <!-- ⚡ ĐĂNG KÝ ĐỂ NHẬN KẾT QUẢ THANH TOÁN TỪ VNPAY -->
        <activity
            android:name=".thanhtoanthanhcongActivity"
            android:exported="true">
            <intent-filter android:label="Thanh toán thành công">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Link này phải khớp với lệnh Redirect trong ASP.NET của bạn -->
                <data android:scheme="myapp" android:host="payment-success" />
            </intent-filter>
        </activity>

    </application>

</manifest>